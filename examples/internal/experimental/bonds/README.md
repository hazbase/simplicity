# Internal Bond Examples

These examples exercise low-level Bond machine and rollover helpers that are intentionally not part of the public `sdk.bonds` API.

Use them only for internal regression, protocol research, or when working directly with `src/internal/experimental/bond.ts`.
